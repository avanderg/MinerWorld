import processing.core.PImage;

import java.util.List;

public class Ash extends Entity {

    public Ash(String id, Point position,
                      List<PImage> images) {
        super(id, position, images);
    }
}
